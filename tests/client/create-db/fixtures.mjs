import { jest } from '@jest/globals';

export const profile = { host: 'db', port: 3306, user: 'u', password: 'p', database: 'app' };
export const bundle = { apiVersion: 'v1', bundleVersion: 'v1', application: 'app', database: 'app', physicalDatabase: 'physical_app', identity: 'runtime', nodeIdentity: 'db', credentials: { username: 'u', password: 'p' }, writer: { host: 'db', port: 3306 }, readers: [{ host: 'read', port: 3306 }, { host: 'read2', port: 3306 }], failover: [], ports: { sql: 3306, http: 8080 }, routes: { primary: [{ host: 'db', port: 3306 }], balanced: [{ host: 'read', port: 3306, weight: 10 }, { host: 'read2', port: 3306, weight: 10 }] }, expiresAt: new Date(Date.now() + 60000).toISOString() };
export const connection = () => ({ query: jest.fn(async (sql) => [[sql]]), execute: jest.fn(async (sql) => [[sql]]), beginTransaction: jest.fn(async () => {}), commit: jest.fn(async () => {}), rollback: jest.fn(async () => {}), release: jest.fn() });
export const driver = (behavior = {}) => ({ createPool: jest.fn(() => ({ query: behavior.query ?? jest.fn(async (sql) => [[sql]]), execute: behavior.execute ?? jest.fn(async (sql) => [[sql]]), getConnection: behavior.getConnection ?? jest.fn(async () => connection()), end: jest.fn(async () => {}) })) });
